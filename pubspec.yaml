name: plantcare_id
description: "A new Flutter project."
publish_to: "none"
version: 0.1.10

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.3.8
  supabase_flutter: ^2.10.3
  flutter_dotenv: ^5.0.2
  sign_in_with_apple: ^7.0.1
  package_info_plus: ^4.0.0
  image_picker: ^0.8.7+5
  camera: ^0.11.0
  flutter_image_compress: ^2.4.0
  mime: ^1.0.2
  http: ^0.13.6
  geolocator: ^9.0.2
  intl: ^0.19.0
  sqflite: ^2.4.1
  path_provider: ^2.1.5
  path: ^1.9.0
  connectivity_plus: ^6.1.2
  crypto: ^3.0.6
  
  # Sprint 3: New dependencies for Guide & Notifications
  firebase_core: ^2.24.3  # ✅ TAMBAHKAN INI UNTUK NOTIFICATION SERVICE
  firebase_messaging: ^14.6.2
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.3
  json_annotation: ^4.8.1
  freezed: ^2.4.2
  freezed_annotation: ^2.2.0
  logger: ^2.0.2
  # HAPUS retrofit dan dio, kita pakai http saja

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # Sprint 3: Code generation dependencies
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  # HAPUS retrofit_generator

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/Hero.webp